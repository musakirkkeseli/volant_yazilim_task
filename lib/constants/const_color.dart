//where their Colors are provided in the app

import 'package:flutter/material.dart';

class AppColor {
  Color mainColor = Colors.orange;
  Color blackColor = Colors.black;
  Color whiteColor = Colors.white;
  Color dullColor = Colors.grey;
  Color? dullColor2 = Colors.grey[300];
  Color? dullColor3 = Colors.grey[100];
  Color deleteColor = Colors.red;
  Color transparentColor = Colors.transparent;
}
